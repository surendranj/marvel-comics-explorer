import Loader from '../src/components/loader';

const HomePage = () => {
    return (
        <div>
            <h1 className="test">Home Page</h1>
            <Loader />
        </div>
    );
};

export default HomePage;
